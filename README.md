# Tlan16 Tap

## How do I install these formulae?

`brew install tlan16/tap/<formula>`

Or `brew tap tlan16/tap` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
